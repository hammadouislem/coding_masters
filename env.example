# Server port
PORT=5000

# MongoDB connection string (replace <username>, <password>, <dbname>)
MONGO_URI="mongodb+srv://coding_masters:azerty123F@cluster0.lfxls5e.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"

# Secret key used to sign JWT tokens (keep this safe!)
JWT_SECRET=secret
